# Local development. Points at the API started by `npm run dev` (or
# `npm run dev:demo`) from the repository root.
REACT_APP_API_URL=http://localhost:5000/api
REACT_APP_ASSET_URL=http://localhost:5000
