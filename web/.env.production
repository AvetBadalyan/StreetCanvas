# Production build. Baked into the JavaScript bundle at build time, so these are
# public values by definition - never put a secret in a VITE_* variable.
VITE_API_URL=https://streetcanvas-api.vercel.app/api
VITE_ASSET_URL=https://streetcanvas-api.vercel.app
