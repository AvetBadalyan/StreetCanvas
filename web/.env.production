# Production build. Baked into the JavaScript bundle at build time, so these are
# public values by definition - never put a secret in a REACT_APP_* variable.
REACT_APP_API_URL=https://streetcanvas-api.vercel.app/api
REACT_APP_ASSET_URL=https://streetcanvas-api.vercel.app
